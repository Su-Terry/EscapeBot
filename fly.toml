# fly.toml file for escape-bot-app
app = "escape-bot-app"
primary_region = "nrt"  # Tokyo, 對台灣 latency 最低
kill_signal = "SIGINT"
kill_timeout = 5

[build]
  dockerfile = "Dockerfile"

[mounts]
  source = "escape_data"
  destination = "/app/User"

[[vm]]
  size = "shared-cpu-1x"
  memory = "256mb"